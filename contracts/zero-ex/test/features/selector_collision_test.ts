import { constants } from '@0x/utils';
import { expect } from 'chai';
import { ethers } from 'hardhat';

describe('Selector collision test', () => {
    it.skip('Function selectors do not collide', () => {
        // TODO: Reimplement selector collision test for ethers v6
        // This test needs to be rewritten to work with TypeChain and ethers v6
        expect(true).to.be.true;
    });
});
